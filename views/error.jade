//- error.jade
    -The error page

doctype html

html(lang='en')
    //- include the head (metadata, css, js, title)
    include ./partials/head

    body(class='body_centered', onload='countdown(4)')
        #error()
          h1 #{status}
          h2 #{message}
          p(id='timer')
